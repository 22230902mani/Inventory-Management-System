const config = {
    // CHANGE THIS TO YOUR LIVE BACKEND URL WHEN DEPLOYED
    // Example: "https://inventory-app-backend.onrender.com"
    API_BASE_URL: import.meta.env.VITE_API_URL || "http://localhost:6700",
};

export default config;
